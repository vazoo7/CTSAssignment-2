import React from 'react';

const MainStudentlogo = (props) => {
    return (
        <div className="collageContainer">
        <img src={props.src} alt={props.alt}/>

     </div>
    );
};

export default MainStudentlogo;